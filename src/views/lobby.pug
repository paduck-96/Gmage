extends base
block content 
    div
        h4 게임 페이지입니다!
        if loggedInUser
            h6 #{loggedInUser.nickname}님 반갑습니다!
            a(href="/logout") 로그아웃
    .jsRooms 
        form#jsCreateRoom
            input(type="text", placeholder="참여하려는 방 이름...")
        .roomContainer
            ul.roomList#jsRooms